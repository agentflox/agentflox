# Deep Integrations — copy into apps/backend/.env
# Generate encryption key: openssl rand -hex 32

# ── Credential vault (required for production) ───────────────────────────────
# Dedicated key — do NOT reuse MODEL_CREDENTIALS_ENCRYPTION_KEY
INTEGRATION_CREDENTIALS_ENCRYPTION_KEY=replace-with-openssl-rand-hex-32-output

# ── OAuth providers (same client ids as frontend login apps) ─────────────────
GITHUB_CLIENT_ID=your-github-oauth-app-client-id
GITHUB_CLIENT_SECRET=your-github-oauth-app-client-secret

SLACK_CLIENT_ID=your-slack-app-client-id
SLACK_CLIENT_SECRET=your-slack-app-client-secret

GOOGLE_CLIENT_ID=your-google-cloud-oauth-client-id
GOOGLE_CLIENT_SECRET=your-google-cloud-oauth-client-secret

# ── Google integration rollout (CASA / OAuth verification) ───────────────────
GOOGLE_INTEGRATION_PUBLIC=false
GOOGLE_INTEGRATION_TEST_USER_IDS=

# ── Optional: disable API startup tool sync (default: runs on boot) ──────────
# DISABLE_INTEGRATION_STARTUP_SYNC=true
