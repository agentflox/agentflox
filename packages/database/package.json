{
  "name": "@agentflox/database",
  "version": "1.0.0",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "exports": {
    ".": {
      "import": "./index.ts",
      "types": "./index.ts"
    }
  },
  "scripts": {
    "postinstall": "node -e \"const fs=require('fs');if(fs.existsSync('./prisma/schema.prisma')){const {execSync}=require('child_process');execSync('prisma generate',{stdio:'inherit'})}else{console.log('[database] Skipping prisma generate: schema not found')}\"",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio",
    "db:seed": "tsx prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.16.2",
    "prisma": "^6.16.2"
  },
  "devDependencies": {
    "tsx": "^4.7.0"
  }
}