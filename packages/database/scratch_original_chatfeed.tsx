import React, { useState } from "react"; import {   CheckCircle2, XCircle, Shield, Check, Zap,   AlertTriangle, Loader2, ChevronDown, X, AtSign, Layers, Pencil, } from "lucide-react"; import { cn } from "@/lib/utils"; import { StreamingMessage } from "@/entities/agents/components/StreamingMessage"; import { type SwarmTask } from "./SwarmTaskView";  // 笏笏笏 Types 笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏 export interface ConvMessage {   role: "user" | "assistant";   content: string;   executionId?: string;   swarmEvent?: { type: string; payload: any; timestamp: string } | null;   ts: number;   meta?: { mentions?: any[]; contexts?: any[] } | null; }  export interface SwarmEvent {   sessionId: string;   type: string;   payload: any;   timestamp: string; }  type SessionStatus = "idle" | "running" | "stopped";  // 笏笏笏 HITL Request Card 笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏 export function HitlRequestCard({ task, onApprove, onDeny }: { task: SwarmTask; onApprove: (id: string) => void; onDeny: (id: string) => void }) {   return (     <div className="border-l-4 border-amber-500 bg-amber-50 rounded-r-xl shadow-inner px-4 py-3 w-full">       <div className="flex items-start gap-2 mb-2">         <Shield className="h-4 w-4 text-amber-600 flex-shrink-0 mt-0.5" />         <div className="flex-1 min-w-0">           <span className="text-[10px] font-black text-amber-700 uppercase tracking-widest block mb-0.5">Human Review Required</span>           <p className="text-sm font-semibold text-zinc-900 leading-snug">{task.title}</p>           {task.metadata?.approvalReason && (             <p className="text-[11px] text-amber-800 italic mt-1 leading-snug">{task.metadata.approvalReason}</p>           )}         </div>       </div>       <div className="flex gap-2 mt-2.5">         <button           onClick={() => onApprove(task.id)}           className="flex-1 flex items-center justify-center gap-1.5 py-1.5 bg-amber-500 hover:bg-amber-600 active:bg-amber-700 text-white text-[11px] font-bold rounded-lg transition-colors shadow-sm"         >           <Check className="h-3.5 w-3.5" /> Approve         </button>         <button           onClick={() => onDeny(task.id)}           className="flex-1 flex items-center justify-center gap-1.5 py-1.5 bg-white hover:bg-red-50 active:bg-red-100 text-red-600 text-[11px] font-bold rounded-lg border border-red-200 transition-colors"         >           <X className="h-3.5 w-3.5" /> Deny         </button>       </div>     </div>   ); }  // 笏笏笏 Task Result Card 笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏 export function TaskResultCard({ payload, time }: { payload: any; time: string }) {   const [expanded, setExpanded] = useState(false);   const hasResult = !!payload.result;   const isHtml = hasResult && /^\s*<[a-z][\s\S]*>/i.test(payload.result);   return (     <div className="border-l-4 border-emerald-500 bg-emerald-50 rounded-r-xl px-4 py-3 w-full">       <div className="flex items-center justify-between mb-1">         <span className="text-[10px] font-black text-emerald-700 uppercase tracking-widest">Task Completed</span>         <span className="text-[9px] text-emerald-400">{time}</span>       </div>       <p className="font-semibold text-sm text-emerald-900 leading-snug">{payload.taskTitle}</p>       {payload.agentName && <p className="text-[10px] text-emerald-600 mt-0.5">via {payload.agentName}</p>}       {hasResult && (         <div className="mt-2 relative">           <div             className={cn(               "bg-white border border-emerald-100 rounded-lg p-2.5 text-[10.5px] leading-relaxed shadow-sm overflow-hidden transition-all duration-300",               isHtml ? "prose prose-sm max-w-none" : "font-mono text-emerald-800",               !expanded && "max-h-20"             )}             style={!expanded ? { WebkitMaskImage: "linear-gradient(to bottom, black 50%, transparent 100%)", maskImage: "linear-gradient(to bottom, black 50%, transparent 100%)" } : {}}           >             {isHtml ? <div dangerouslySetInnerHTML={{ __html: payload.result }} /> : payload.result}           </div>           <button             onClick={() => setExpanded(e => !e)}             className="mt-1 flex items-center gap-1 text-[10px] text-emerald-600 hover:text-emerald-800 font-semibold transition-colors"           >             <ChevronDown className={cn("h-3 w-3 transition-transform", expanded && "rotate-180")} />             {expanded ? "Collapse" : "Show full output"}           </button>         </div>       )}     </div>   ); }  // 笏笏笏 Coordinator Tick 笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏 export function CoordinatorTick({ label, time }: { label: string; time: string }) {   return (     <div className="flex items-center gap-2 py-0.5 px-1">       <div className="flex-1 h-px bg-zinc-100" />       <span className="text-[9px] text-zinc-400 font-mono whitespace-nowrap">{label} ﾂｷ {time}</span>       <div className="flex-1 h-px bg-zinc-100" />     </div>   ); }  // 笏笏笏 Inter-Agent Message 笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏 export function InterAgentMessage({ payload, time }: { payload: any; time: string }) {   const sender = payload.fromName || (payload.from ? payload.from.slice(0, 8) : "Agent");   const receiver = payload.toName || (payload.to ? payload.to.slice(0, 8) : "Agent");   return (     <div className="ml-6 border-l-2 border-teal-200 pl-3">       <div className="bg-teal-50 rounded-xl rounded-bl-none px-3 py-2 text-xs">         <div className="flex items-center justify-between mb-1">           <span className="font-bold text-[10px] text-teal-700 uppercase tracking-wide">             {sender} <span className="text-teal-400">竊・/span> {receiver}           </span>           <span className="text-[9px] text-teal-400">{time}</span>         </div>         <p className="text-teal-900 whitespace-pre-wrap text-[11px] leading-relaxed">{payload.detail || payload.content}</p>       </div>     </div>   ); }  // 笏笏笏 Final Output Bubble 笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏 export function FinalOutputBubble({ content }: { content: string }) {   return (     <div className="flex flex-col items-end gap-1">       <span className="text-[10px] text-emerald-600 font-bold flex items-center gap-1 pr-1">         <CheckCircle2 className="h-3 w-3" /> Swarm complete       </span>       <div className="rounded-2xl bg-zinc-900 text-zinc-50 px-4 py-3 text-sm whitespace-pre-wrap max-w-[90%] ring-2 ring-indigo-500/40 shadow-lg">         {content}       </div>     </div>   ); }  // 笏笏笏 Active Processing Widget 笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏 export function ActiveProcessingWidget({ events, status }: { events: SwarmEvent[]; status: SessionStatus }) {   if (status === "idle") return null;   if (status === "stopped") {     return (       <div className="flex items-center gap-1.5 text-[10px] text-emerald-600 font-semibold">         <CheckCircle2 className="h-3 w-3" /> Session complete       </div>     );   }   const latest = events.find(e => e.type === "CYCLE_INSPECT");   if (!latest) return (     <div className="flex items-center gap-1.5 text-[10px] text-indigo-500 font-semibold">       <Loader2 className="h-3 w-3 animate-spin" /> Swarm initializing窶ｦ     </div>   );   const count = latest.payload?.taskCount ?? latest.payload?.taskIds?.length ?? 0;   return (     <div className="flex items-center gap-1.5 text-[10px] text-indigo-600 font-semibold">       <Zap className="h-3 w-3 animate-pulse text-indigo-500" />       {count > 0 ? `Coordinator inspecting ${count} tasks窶ｦ` : "Coordinator scanning backlog窶ｦ"}     </div>   ); }  // 笏笏笏 renderUserMessageTokens 笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏 export function renderUserMessageTokens(content: string, mentions?: any[]): React.ReactNode {   // Regex to match [@Name] or [#Name] or @Name or #Name   const regex = /(\[?[@#][^\]\s]+\]?)/g;      return content.split(regex).map((part, i) => {     if (part.match(/^\[?[@#]/)) {       const isTask = part.includes('#');       const cleanName = part.replace(/[\[\]@#]/g, '');              return (         <span key={i} className={cn(           "inline-flex items-center rounded px-1.5 py-0.5 mx-[1px] font-bold text-[12px] leading-tight align-baseline shadow-sm border",           "bg-purple-50 text-purple-700 border-purple-200"         )}>           {isTask ? <Files className="h-2.5 w-2.5 mr-1" /> : <AtSign className="h-2.5 w-2.5 mr-1" />}           {cleanName}         </span>       );     }     return <span key={i}>{part}</span>;   }); }  // 笏笏笏 SwarmChatFeed 笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏笏 interface CombinedFeedItem {   ts: number;   id: string;   _type: 'message' | 'hitl' | 'tick';   msg?: ConvMessage;   idx?: number;   task?: SwarmTask;   evt?: SwarmEvent; }  interface SwarmChatFeedProps {   combinedFeed: CombinedFeedItem[];   workforceName: string;   editingIndex: number | null;   setEditingIndex: (i: number | null) => void;   setMessages: React.Dispatch<React.SetStateAction<ConvMessage[]>>;   handleApprove: (id: string) => void;   handleDeny: (id: string) => void;   isSending: boolean;   isPolling: boolean;   pollingExecutionId: string | null;   error: string | null;   thinkingSteps: any[];   thinkingStep: any;   thinkingNode: any;   streamingContent: string;   isStreaming: boolean;   bottomRef: React.RefObject<HTMLDivElement>; }  export function SwarmChatFeed({   combinedFeed,   workforceName,   editingIndex,   setEditingIndex,   setMessages,   handleApprove,   handleDeny,   isSending,   isPolling,   pollingExecutionId,   error,   thinkingSteps,   thinkingStep,   thinkingNode,   streamingContent,   isStreaming,   bottomRef, }: SwarmChatFeedProps) {   return (     <div className="max-w-2xl mx-auto space-y-3">       {combinedFeed.length === 0 && !isSending && (         <div className="flex flex-col items-center gap-3 py-16 text-center">           <div className="h-12 w-12 rounded-full bg-indigo-50 flex items-center justify-center">             <span className="text-lg font-bold text-indigo-500">{workforceName.slice(0, 1)}</span>           </div>           <p className="text-sm font-semibold text-zinc-700">{workforceName}</p>           <p className="text-xs text-zinc-400 max-w-xs">Type a task below to run the workforce. Start the swarm to watch agents collaborate in real-time.</p>         </div>       )}        {combinedFeed.map((item) => {         if (item._type === 'tick') {           const e = item.evt!;           const time = new Date(e.timestamp).toLocaleTimeString();           if (e.type === "CYCLE_INSPECT") {             const count = e.payload?.taskCount ?? e.payload?.taskIds?.length ?? 0;             return <CoordinatorTick key={item.id} label={count > 0 ? `${count} tasks in backlog` : "coordinator cycle"} time={time} />;           }           return (             <div key={item.id} className="flex items-start gap-2">               <AlertTriangle className="h-4 w-4 text-orange-500 mt-0.5 flex-shrink-0" />               <div className="text-xs text-orange-600 bg-orange-50 border border-orange-100 rounded-lg px-3 py-2 flex-1">                 <span className="font-bold">Cycle error</span> ﾂｷ {e.payload?.detail} ﾂｷ <span className="text-orange-400">{time}</span>               </div>             </div>           );         }          if (item._type === 'hitl') {           return (             <div key={item.id} className="w-full">               <HitlRequestCard task={item.task!} onApprove={handleApprove} onDeny={handleDeny} />             </div>           );         }          if (item._type === 'message') {           const msg = item.msg!;           const idx = item.idx!;            if (msg.swarmEvent) {             const { type, payload, timestamp } = msg.swarmEvent;             const time = new Date(timestamp).toLocaleTimeString();              if (type === 'SESSION_STARTED') return (               <div key={item.id} className="flex items-center gap-3 py-1">                 <div className="flex-1 h-px bg-indigo-100" />                 <div className="flex items-center gap-1.5 px-3 py-1 rounded-full bg-indigo-50 border border-indigo-200 text-[11px] font-bold text-indigo-700">                   <Zap className="h-3 w-3" />{payload.label || '噫 Swarm started'}                   <span className="text-indigo-400 font-normal ml-1">{time}</span>                 </div>                 <div className="flex-1 h-px bg-indigo-100" />               </div>             );              if (type === 'SESSION_STOPPED') return (               <div key={item.id} className="flex items-center gap-3 py-1">                 <div className="flex-1 h-px bg-red-100" />                 <div className="flex items-center gap-1.5 px-3 py-1 rounded-full bg-red-50 border border-red-200 text-[11px] font-bold text-red-600">                   <XCircle className="h-3 w-3" />{payload.label || '尅 Session stopped'}                   <span className="text-red-300 font-normal ml-1">{time}</span>                 </div>                 <div className="flex-1 h-px bg-red-100" />               </div>             );              if (type === 'USER_DIRECTED_MESSAGE') {               const mentions: any[] = payload.mentions || [];               const agentMentions = mentions.filter(m => m.type === 'agent');               const taskMentions = mentions.filter(m => m.type === 'task');               const toName = payload.toName || 'Swarm';               return (                 <div key={item.id} className="flex items-center gap-2 my-1">                   <div className="flex-1 h-px bg-blue-100" />                   <div className="flex items-center gap-1.5 px-3 py-1.5 rounded-full bg-blue-50 border border-blue-200 text-[11px] font-semibold text-blue-700 max-w-[85%] flex-shrink-0">                     <AtSign className="h-3 w-3 text-blue-500 flex-shrink-0" />                     <span className="text-blue-500 font-bold">User</span>                     <span className="text-blue-400">竊・/span>                     {agentMentions.map((m, i) => (                       <span key={i} className="bg-blue-100 text-blue-800 font-bold px-1.5 py-0.5 rounded text-[10px]">@{m.name}</span>                     ))}                     {taskMentions.map((m, i) => (                       <span key={i} className="bg-indigo-100 text-indigo-800 font-bold px-1.5 py-0.5 rounded text-[10px]">#{m.name}</span>                     ))}                     {agentMentions.length === 0 && taskMentions.length === 0 && <span className="font-bold">{toName}</span>}                     <span className="text-blue-300 font-normal ml-1">{time}</span>                   </div>                   <div className="flex-1 h-px bg-blue-100" />                 </div>               );             }              if (type === 'COORDINATOR_INSPECT') return (               <div key={item.id} className="flex items-start gap-2">                 <div className="h-7 w-7 rounded-full bg-violet-100 flex items-center justify-center flex-shrink-0 mt-0.5">                   <Zap className="h-3.5 w-3.5 text-violet-600" />                 </div>                 <div className="bg-violet-50 border border-violet-100 rounded-2xl rounded-tl-sm px-3 py-2 text-xs text-violet-800 flex-1 max-w-[90%]">                   <div className="flex items-center justify-between mb-1">                     <span className="font-bold text-[10px] text-violet-600 uppercase tracking-wide">Coordinator</span>                     <span className="text-[9px] text-violet-300">{time}</span>                   </div>                   <p className="font-semibold text-violet-900 mb-1">{payload.label}</p>                   {payload.detail && <pre className="text-violet-700 whitespace-pre-wrap text-[10.5px] leading-relaxed">{payload.detail}</pre>}                 </div>               </div>             );              if (type === 'COORDINATOR_ASSIGN') return (               <div key={item.id} className="flex items-start gap-2">                 <div className="h-7 w-7 rounded-full bg-blue-100 flex items-center justify-center flex-shrink-0 mt-0.5">                   <CheckCircle2 className="h-3.5 w-3.5 text-blue-600" />                 </div>                 <div className="bg-blue-50 border border-blue-100 rounded-2xl rounded-tl-sm px-3 py-2 text-xs flex-1 max-w-[90%]">                   <div className="flex items-center justify-between mb-1">                     <span className="font-bold text-[10px] text-blue-600 uppercase tracking-wide">Task Assignment</span>                     <span className="text-[9px] text-blue-300">{time}</span>                   </div>                   <p className="font-semibold text-blue-900 mb-0.5">{payload.taskTitle}</p>                   <p className="text-blue-600">{payload.detail}</p>                 </div>               </div>             );              if (type === 'AGENT_PROGRESS') return (               <div key={item.id} className="flex items-start gap-2">                 <div className="h-7 w-7 rounded-full bg-amber-100 flex items-center justify-center flex-shrink-0 mt-0.5">                   <Loader2 className="h-3.5 w-3.5 text-amber-600 animate-spin" />                 </div>                 <div className="bg-amber-50 border border-amber-100 rounded-2xl rounded-tl-sm px-3 py-2 text-xs flex-1 max-w-[90%]">                   <div className="flex items-center justify-between mb-1">                     <span className="font-bold text-[10px] text-amber-600 uppercase tracking-wide">Agent {payload.from?.slice(0, 8) ?? ''}</span>                     <span className="text-[9px] text-amber-300">{time}</span>                   </div>                   <p className="text-amber-900">{payload.detail}</p>                 </div>               </div>             );              if (type === 'TASK_COMPLETED') return (               <div key={item.id} className="w-full">                 <TaskResultCard payload={payload} time={time} />               </div>             );              if (type === 'TASK_FAILED') return (               <div key={item.id} className="flex items-start gap-2">                 <div className="h-7 w-7 rounded-full bg-red-100 flex items-center justify-center flex-shrink-0 mt-0.5">                   <XCircle className="h-3.5 w-3.5 text-red-500" />                 </div>                 <div className="border-l-4 border-red-500 bg-red-50 rounded-r-xl px-3 py-2.5 text-xs flex-1">                   <div className="flex items-center justify-between mb-1">                     <span className="font-bold text-[10px] text-red-600 uppercase tracking-widest">Task Failed</span>                     <span className="text-[9px] text-red-300">{time}</span>                   </div>                   <p className="font-semibold text-red-900 mb-0.5">{payload.taskTitle}</p>                   <p className="text-red-600 text-[11px]">{payload.error}</p>                 </div>               </div>             );              if (type === 'INTER_AGENT_MSG') return (               <div key={item.id}>                 <InterAgentMessage payload={payload} time={time} />               </div>             );              if (type === 'HITL_REQUEST' && payload?.taskId) return (               <div key={item.id} className="w-full">                 <HitlRequestCard                   task={{ id: payload.taskId, title: payload.taskTitle || 'Approval Required', status: 'PENDING_APPROVAL', priority: 'HIGH', createdAt: timestamp, metadata: { approvalReason: payload.reason } }}                   onApprove={handleApprove}                   onDeny={handleDeny}                 />               </div>             );              if (type === 'CYCLE_IDLE') return (               <CoordinatorTick key={item.id} label={payload.label || 'coordinator idle'} time={time} />             );              if (type === 'CYCLE_ERROR') return (               <div key={item.id} className="flex items-start gap-2">                 <AlertTriangle className="h-4 w-4 text-orange-500 mt-0.5 flex-shrink-0" />                 <div className="text-xs text-orange-600 bg-orange-50 border border-orange-100 rounded-lg px-3 py-2 flex-1">                   <span className="font-bold">Cycle error</span> ﾂｷ {payload.detail} ﾂｷ <span className="text-orange-400">{time}</span>                 </div>               </div>             );              // Fallback for unknown event types             return (               <div key={item.id} className="flex items-start gap-2">                 <div className="h-6 w-6 rounded-full bg-zinc-100 flex items-center justify-center flex-shrink-0">                   <Zap className="h-3 w-3 text-zinc-400" />                 </div>                 <div className="bg-zinc-50 border border-zinc-100 rounded-2xl rounded-tl-sm px-3 py-2 text-xs text-zinc-600 max-w-[85%]">                   <span className="font-bold text-[10px] text-zinc-400 block mb-0.5">{type}</span>                   {msg.content}                   <span className="text-[9px] text-zinc-300 ml-2">{time}</span>                 </div>               </div>             );           }            // Plain user / assistant chat bubble           return (             <div key={item.id} className={cn("flex items-start gap-2 group", msg.role === "assistant" && "flex-col items-end")}>               {msg.role === "user" ? (                 <>                   <div className="flex-1 rounded-2xl rounded-tl-sm bg-white border border-zinc-200 px-4 py-3 shadow-sm flex flex-col items-start min-w-0">                     {editingIndex === idx ? (                       <textarea defaultValue={msg.content} autoFocus                         className="w-full min-h-[60px] text-sm text-zinc-900 resize-none outline-none"                         onBlur={e => { setMessages(p => p.map((m, i) => i === idx ? { ...m, content: e.target.value } : m)); setEditingIndex(null); }}                         onKeyDown={e => { if (e.key === "Enter") (e.target as any).blur(); }} />                     ) : (                       <p className="text-sm text-zinc-900 whitespace-pre-wrap">{renderUserMessageTokens(msg.content, msg.meta?.mentions)}</p>                     )}                     {msg.meta && (msg.meta.mentions?.length > 0 || msg.meta.contexts?.length > 0) && editingIndex !== idx && (                       <div className="flex flex-wrap items-center gap-1.5 mt-2.5">                         {(msg.meta.mentions || []).map((m: any, i: number) => (                           <div key={`m-${i}`} className="flex items-center gap-1 bg-blue-50 text-blue-700 font-bold px-2 py-0.5 rounded shadow-sm border border-blue-200 text-[10px] tracking-wide uppercase">                             <AtSign className="h-3 w-3 text-blue-600" />                             <span>{m.name || 'Mention'}</span>                           </div>                         ))}                         {(msg.meta.contexts || []).map((c: any, i: number) => (                           <div key={`c-${i}`} className="flex items-center gap-1 bg-indigo-50 text-indigo-700 font-bold px-2 py-0.5 rounded shadow-sm border border-indigo-200 text-[10px] tracking-wide uppercase">                             <Layers className="h-3 w-3 text-indigo-600" />                             <span>{c.name || 'Context'}</span>                           </div>                         ))}                       </div>                     )}                   </div>                   {editingIndex !== idx && (                     <button onClick={() => setEditingIndex(idx)} className="opacity-0 group-hover:opacity-100 p-1.5 rounded hover:bg-zinc-100 text-zinc-400 transition-opacity">                       <Pencil className="h-3 w-3" />                     </button>                   )}                 </>               ) : (                 msg.executionId                   ? <FinalOutputBubble content={msg.content} />                   : <div className="rounded-2xl bg-zinc-900 text-zinc-50 px-4 py-3 text-sm whitespace-pre-wrap max-w-[90%]">{msg.content}</div>               )}             </div>           );         }          return null;       })}        {isSending && (         <div className="flex items-start justify-end">           <div className="rounded-2xl bg-zinc-900 text-zinc-50 px-4 py-3 text-sm max-w-[90%]">             <StreamingMessage               thinkingSteps={thinkingSteps}               currentStep={thinkingStep}               currentNode={thinkingNode}               streamingContent={streamingContent}               isStreaming={isStreaming}               agentLabel={workforceName}             />           </div>         </div>       )}        {isPolling && !isSending && (         <div className="flex items-start justify-end">           <div className="rounded-2xl bg-zinc-900 text-zinc-50 px-4 py-3 text-sm max-w-[90%]">             <div className="flex items-center gap-2 text-zinc-300">               <Loader2 className="h-3.5 w-3.5 animate-spin" />               {pollingExecutionId ? `Running (${pollingExecutionId.slice(0, 8)}窶ｦ) 窶・finalizing窶ｦ` : "Finalizing窶ｦ"}             </div>           </div>         </div>       )}        {error && <div className="rounded-lg border border-red-200 bg-red-50 px-4 py-2 text-sm text-red-700">{error}</div>}       <div ref={bottomRef} />     </div>   ); }